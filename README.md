<div align="center">

<img src="backend/logo.png" alt="Logo" width="100" height="100">

# 🛡️ Aegis Auth System (Pro)

**企业级 PHP 多租户卡密验证与软件授权系统**

[![PHP Version](https://img.shields.io/badge/php-%3E%3D7.4-777BB4.svg?style=flat-square&logo=php&logoColor=white)](https://www.php.net/)
[![Database](https://img.shields.io/badge/sqlite-embedded-003B57.svg?style=flat-square&logo=sqlite&logoColor=white)](https://www.sqlite.org/)
[![License](https://img.shields.io/badge/license-MIT-green.svg?style=flat-square)](LICENSE)
[![Security](https://img.shields.io/badge/security-hardened-blue.svg?style=flat-square&logo=security&logoColor=white)]()
[![Size](https://img.shields.io/badge/size-%3C2MB-orange.svg?style=flat-square)]()

[功能特性](#-功能特性) • [安装部署](#-安装部署) • [API 文档](#-api-接口文档) • [接入示例](#-多语言接入示例) • [演示截图](#-系统截图)

</div>

---

## 📖 项目简介

**GuYi Aegis Auth** 是一款专为独立开发者和中小型软件工作室打造的轻量级、高性能授权管理系统。

不同于传统的验证系统，Aegis 采用了**零依赖架构**（无需 MySQL，无需 Redis），基于原生 PHP + SQLite 构建，上传即可运行。系统内置了**多应用（多租户）管理**功能，允许你在一个后台同时管理多个不同软件的卡密系统。
👉 **完整开发文档与使用手册： [http://aegis.可爱.top/](http://aegis.可爱.top/)**
## ✨ 核心特性

### 🛡️ 安全与防护
- **金融级安全架构**：核心配置分离，密码 Hash 加密存储，全站 PDO 防注入。
- **设备指纹绑定**：基于 `机器码 + IP + UA` 的多维设备指纹，杜绝卡密共享与多开。
- **前端防调试**：Web 端验证页面内置反右键、反 F12、防开发者工具检测代码。
- **API 速率限制**：内置 IP Rate Limiting（`api.php`），防止暴力破解与 CC 攻击。
- **CSRF & XSS 防护**：后台全表单 CSRF Token 校验，Cookie 强签名验证。

### ⚡ 高效管理
- **多应用接入**：支持创建多个 App，每个应用拥有独立的 App Key 和卡密池。
- **批量作业**：支持批量生成、批量导出、批量解绑设备、批量加时。
- **可视化仪表盘**：实时监控活跃设备、库存占比、鉴权日志审计。
- **零配置部署**：自动创建 SQLite 数据库，无需手动导入 SQL。

### 🎨 现代交互
- **Glassmorphism UI**：前后台采用流行的“毛玻璃”拟态风格设计，视觉体验极佳。
- **RESTful API**：标准 JSON 接口，支持 C#、Python、Lua、易语言、Android 等全平台接入。

---

## 🚀 安装部署

### 环境要求
- PHP >= 7.4
- 扩展：`pdo_sqlite`, `gd`, `openssl`
- 目录权限：`/data` 目录需要读写权限 (777)

### 快速开始
1. **上传文件**：将所有文件上传至服务器 Web 目录。
2. **设置权限**：确保 `/data` 文件夹具有写入权限。
3. **修改密钥**：打开 `config.php`，修改 `SYS_SECRET` 为一段随机字符串（**重要！用于Cookie签名**）。
4. **访问后台**：
   - 地址：`http://your-domain.com/cards.php`
   - 默认账号：`admin`
   - 默认密码：`admin123`
   - *（首次登录后请务必在“系统设置”中修改密码）*

---

## 🔌 API 接口文档

系统提供统一的 RESTful API 接口，供客户端软件调用。

- **接口地址**: `http://你的域名/Verifyfile/api.php`
- **请求方式**: `POST` (推荐) 或 `GET`
- **Content-Type**: `application/json` 或 `application/x-www-form-urlencoded`

### 1. 验证/激活/心跳接口

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `card` | string | 是 | 用户输入的卡密代码 |
| `device` | string | 否 | 客户端机器码（建议提交以绑定设备） |
| `app_key`| string | 否 | 应用密钥（后台创建应用时获取，留空则验证通用卡） |

**成功响应 (200 OK)：**

```json
{
    "code": 200,
    "msg": "OK",
    "data": {
        "status": "active",
        "expire_time": "2026-12-31 23:59:59",
        "remaining_seconds": 86400,
        "device_id": "a1b2c3d4..."
    }
}